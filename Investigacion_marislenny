El react router es un interfaz de usuario sincronizada a una URL, tiene una API simple con funciones poderosas como cuando se da y una carga diferida de codigo, una coincidencia de rutas y manejo de transición, se puede definir como un enrutamiento estándar para React, de los documentos, el enrutamiento es el proceso por el cual la ruta o una o alguna parte de ella ya está determinada como la misma.
